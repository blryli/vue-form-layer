import PopoverContent from '../popover/popover-content';

/* istanbul ignore next */
PopoverContent.install = function(Vue) {
  Vue.component(PopoverContent.name, PopoverContent);
};

export default PopoverContent;
